# netlify.toml — CreatorCenter.id AI ViralHook
[build]
  publish = "."
  functions = "netlify/functions"

# Optional: pretty path /api/generate-hooks -> the function
[[redirects]]
  from = "/api/generate-hooks"
  to   = "/.netlify/functions/generate-hooks"
  status = 200
